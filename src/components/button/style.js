import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({

    text: {
        textTransform: 'uppercase',
        fontSize: 16,
        fontWeight: '600',
        color: '#fff',
        marginTop: 10,
        textAlign: 'center'
    }

})